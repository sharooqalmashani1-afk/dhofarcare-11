# DhofarCare — Static (GitHub Pages) + Hosted Payment + Google Sheets
Edit `config.js` with:
- `SHEET_WEBAPP_URL`: your Google Apps Script Web App URL (exec).
- `PAYMENT_URL`: PayTabs Hosted Payment Page link (or Stripe Payment Link).
- `WHATSAPP_NUMBER`: digits only.

Publish: push files to GitHub, enable Pages from `main`/root.
